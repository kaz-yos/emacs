# -*- mode: snippet -*-
# name: clogit
# key: clogit
# --
res. <- clogit(formula    = + strata(id),
               data       = ,
               ## weights = ,
               ## subset  = ,
               method     =c("exact", "approximate", "efron", "breslow")[1])